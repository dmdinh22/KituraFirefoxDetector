import XCTest
@testable import KituraFirefoxDetector

final class KituraFirefoxDetectorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(KituraFirefoxDetector().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
